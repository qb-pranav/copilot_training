# copilot_training

This repository contains a Playwright demo script that opens the Playwright homepage and validates key page content.

## Run the demo

1. Install dependencies with `npm install`.
2. Install the Chromium browser used by Playwright with `npm run install:browsers`.
3. Run the script with `npm start`.
