# copilot_training

This repository contains a small Playwright demo script that opens the Playwright homepage and validates a few key homepage elements.

## Run the demo

1. Install dependencies with `npm install`.
2. Run the script with `npm start`.
